## Session - 2026-05-16

**Completed (Kyle email feedback round 2):**
- Partner marquee: removed hyperlinks per Kyle — logos now just scroll, no links, no hover/box chrome. Plain white logos on the track.
- /projects: removed the YouTube video gallery (Kyle clarified those videos are social-media content, not project videos — project videos don't exist). Restored the previous-website card format: property image, name, location, unit/room count, 1-2 sentence write-up. Card structure pulled from git (commit 9cc734b). Data array left empty with a documented example shape + clean "coming soon" empty state — Kyle is sending real project content.
- Service sub-pages: fixed broken hero images on all 5 (/services/digital-solutions, enterprise, hospitality, infrastructure-consulting, multi-family). They referenced /software-development-coding.jpg, /luxury-hotel-lobby.jpg, /enterprise-data-center.jpg, /network-infrastructure-datacenter.jpg, /modern-apartment-building.jpg — none of which exist in public/. Repointed each to an existing image. This is the "broken header image in each section" Kyle reported.

**Files Changed:**
- components/redesign/home/partner-marquee.tsx — removed <a> links, hover chrome, href data
- app/(main)/projects/page.tsx — removed video gallery, restored project card grid (image/name/units/blurb), empty state
- app/(main)/services/{digital-solutions,enterprise,hospitality,infrastructure-consulting,multi-family}/page.tsx — fixed broken hero img src

**Decisions:**
- Kyle confirmed: no hyperlinks on partner logos, no project videos. /projects goes back to property cards.
- Service sub-pages: user chose "just fix broken images" — left old off-brand design intact, only unbroke the images. Kyle reviewing this evening; he flagged these pages "need to be built out further" — likely a follow-up.

**Added later same session:**
- New /videos page (app/(main)/videos/page.tsx) — curated 3x3 grid of YouTube embeds from Kyle's channel @halo_integration (channel ID UCEY19QSOhT8QZjPT0gv5u5w) + "View all on YouTube" button. Curated approach chosen over auto-feed (static site, no API key, marketing site should curate). 9 videos hand-picked from the channel's 22; swap IDs in the `videos` array to re-curate. Uses youtube-nocookie.com + loading="lazy".
- "Videos" link added to nav (glass-navigation.tsx) and footer company list (footer.tsx).
- Confirmed: NO real project data exists locally — searched whole repo + ~/Downloads, ~/Desktop, ~/Documents. The Dec 22 WORK-LOG notes a folder Kyle named "Won Project Images for Website" that was never added. /projects has only ever had fake placeholder cards. Real data must come from Kyle / that folder.

**Projects page populated (later same session):**
- Kyle sent project content as public/Closed Projects.docx (53MB, 18 projects + embedded images).
- Extracted text + images from the .docx (it's a zip: word/document.xml for text, media/ for images, word/_rels/document.xml.rels maps rId→media file; images appear in doc order, one per project).
- Optimized all 18 images via `sips -s format jpeg -Z 1600` (some originals were 11-13MB PNGs) → public/projects/*.jpg, 8.2MB total (~450KB avg).
- Populated projects[] array in app/(main)/projects/page.tsx with all 18 (name, category, location, unit/room count, scope blurb, image). Doc order kept (alphabetical).
- Card: location line now conditional (300 E 36th has no city), images lazy-load + subtle hover zoom.
- Deleted public/Closed Projects.docx — 53MB source file, should not ship in the static export.

**Next Steps:**
- [ ] Kyle reviewing service sub-pages this evening — decide whether to rebuild them in the redesign dark theme (they're currently old off-brand design)
- [ ] Re-curate /videos list if Kyle wants specific videos featured
- [ ] Kyle to send URLs for Blueport, BSR, KISD, MI logos (carried over)
- [ ] Team photos for About page when ready (carried over)

**Gotchas:**
- The 5 service sub-pages (/services/<slug>) are pre-redesign design — light theme, Button/Card from old design system. They don't match the rest of the site. Footer links point to them.
- YouTube channel pages are JS-rendered — WebFetch can't see videos. Get channel ID via `curl ... | grep externalId`, then titles+IDs via the RSS feed `youtube.com/feeds/videos.xml?channel_id=UC...` (latest 15).

---

## Session - 2026-03-01

**Completed:**
- Applied all partner email feedback to /redesign version
- Promoted /redesign to the live main site (halointegration.com)
- Fixed Cloudflare Pages CI (had been failing for 2 months due to invalid `build` key in wrangler.json)
- Fixed git remote URL (repo renamed from halointegraion → site-halointegration)
- Deleted duplicate Cloudflare Pages project (halointegraion typo project)
- Fixed wrangler.json project name to match live project (halointegration)

**Email Changes Applied (to redesign → now live):**
- Home hero: icons bigger, Structured Cabling moved to top of ring, "Infrastructure" → "Technology Strategy & Consulting" (Briefcase icon), removed "Low Voltage & Digital Infrastructure" badge, updated tagline last sentence
- Services page: new tagline "End-to-End Technology Services", new sub-tagline, updated Multi-Family/Hospitality/Enterprise descriptions, brighter subtext
- Contact page: address corrected to STE #103
- Logo: updated to MASTER LOGO_ALT_WHT_V2.png (vertical white, 500x500), larger in nav

**Additional Changes:**
- About page: removed "Meet the Experts" placeholder section
- Partner marquee: logos now link to vendor sites, bigger (h-10), more visible (opacity-60)
- Redesign CSS imported into root layout so .text-gradient, .animate-marquee etc. work everywhere

**Files Changed (key):**
- components/redesign/home/hero-section.tsx - icon ring, badge, tagline
- components/redesign/home/partner-marquee.tsx - linked logos, visibility
- components/redesign/layout/glass-navigation.tsx - new logo, root links
- components/redesign/layout/footer.tsx - removed "Back to Original" link, root links
- app/(main)/layout.tsx - now uses GlassNavigation + FooterRedesign
- app/(main)/page.tsx - now imports redesign home components
- app/(main)/about/page.tsx - redesign version, Meet the Experts removed
- app/(main)/services/page.tsx - redesign version with email updates
- app/(main)/contact/page.tsx - redesign version, STE #103
- app/(main)/projects/page.tsx - new page (from redesign)
- app/layout.tsx - imports redesign globals.css
- app/redesign/services/page.tsx - email updates applied
- wrangler.json - fixed project name, removed invalid build key
- public/MASTER LOGO_ALT_WHT_V2.png - new logo file added

**Decisions:**
- Kept /redesign route intact (now has root links, effectively deprecated)
- Deploy strategy: wrangler pages deploy out (direct static deploy) because CI was unreliable; CI now fixed so future git pushes should auto-deploy
- Used `output: 'export'` static export mode (already configured in next.config)

**Pending:**
- [ ] Team photos for About page when ready (section removed for now)

---

## Session - 2026-05-10

**Completed:**
- Reordered services per Kyle: Digital Infra → Network/Wi-Fi → Surveillance → Access Control → Prop-Tech → A/V → VoIP
- Wired real service images from /public/services/ into services page
- Replaced old 6 partner logos with 17 new logos from Kyle; trimmed to 8 clean ones that render correctly with white treatment
- Bumped hero icon ring from 48px → 64px (Kyle asked again)
- Built /projects video gallery section — ready for YouTube IDs, fake placeholder data removed
- Fixed broken asset paths (spaces in folder names "Partner Logos", "Services Page" caused 404s in static export — renamed to partner-logos/, services/)
- Fixed partner marquee: uniform w-44 h-20 boxes, object-contain, brightness-0 invert for consistent white logos
- Removed Elauwit, Groove, Hilton, IHG, MAA from marquee (blank boxes — logos incompatible with white filter treatment)

**Files Changed:**
- app/(main)/services/page.tsx — service reorder, real images, Image import
- app/(main)/projects/page.tsx — removed fake data/stats, added YouTube video gallery structure
- components/redesign/home/hero-section.tsx — icon ring size bump
- components/redesign/home/partner-marquee.tsx — new logos, uniform boxes, white filter, trimmed list

**Decisions:**
- Videos go on /projects page as YouTube embeds (Kyle uploads to YouTube, sends links)
- White filter (brightness-0 invert) kept for logo consistency — logos with non-transparent backgrounds don't work with this approach, remove those
- No fake stats or placeholder project cards on /projects — clean empty state until Kyle provides real data

**Next Steps:**
- [ ] Kyle to send URLs for Blueport, BSR, KISD, MI logos
- [ ] Kyle to create YouTube channel and upload videos, send links
- [ ] Wire YouTube IDs into /projects video gallery
- [ ] Real project names/details from Kyle if he wants project cards populated
- [ ] Team photos for About page when ready

**Gotchas:**
- Spaces in public/ folder names and filenames cause 404s in Next.js static export — always use kebab-case for asset folders and files
- brightness-0 invert CSS filter only works for logos with transparent backgrounds — JPEGs and PNGs with solid backgrounds render as solid white rectangles

**Gotchas:**
- wrangler.json `build.command` key is NOT valid for Pages projects — caused 2 months of CI failures
- Next.js static export outputs to `out/` not `.next/` — Cloudflare Pages build output dir must be set to `out`
- Cloudflare CDN caches aggressively — hard refresh (Cmd+Shift+R) needed to see changes
- Logo file has spaces in name ("MASTER LOGO_ALT_WHT_V2.png") — works fine with Next.js Image src
- After promoting redesign to main, all /redesign/ hrefs in components needed updating to root paths (used sed batch replace)
